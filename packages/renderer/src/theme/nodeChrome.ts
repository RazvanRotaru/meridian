/** Shared paint/layout footprint for the trailing expand/collapse control on every graph node. */
export const NODE_DISCLOSURE_SIZE = 22;

/** Minimum total frame height when expansion has no graph children and renders the shared empty state. */
export const NODE_EMPTY_EXPANSION_HEIGHT = 82;
