import type { ReactElement } from "react";

/** Page footer. Renders nothing but host markup. */
export function Footer(): ReactElement {
  return (
    <footer className="footer">
      <small>© 2026 shopfront — a fixture, not a store.</small>
    </footer>
  );
}
